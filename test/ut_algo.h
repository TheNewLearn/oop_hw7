#include <gtest/gtest.h>
#include "../src/algo.h"
#include "../src/cat.h"
#include "../src/pet.h"
#include "../src/dog.h"
#include <string>
#include <vector>





TEST(PETTEST, wei)
{
    cat c1("Hello_Kitty", 5.0);
    cat c2("melo", 4.2);
    cat c3("qq", 2.5);
    
    

    //ASSERT_EQ(pet_weigh, maxPet(pet_weigh.begin(), pet_weigh.end(), mycom));
}
